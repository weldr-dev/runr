{
  "agent": {
    "name": "tactical-grid-ambiguous",
    "version": "1"
  },
  "repo": {
    "default_branch": "main"
  },
  "scope": {
    "allowlist": [
      "apps/tactical-grid-ambiguous/**"
    ],
    "denylist": ["**/node_modules/**"],
    "lockfiles": ["package-lock.json", "pnpm-lock.yaml", "yarn.lock"]
  },
  "verification": {
    "tier0": ["npm install", "npm run lint", "npm run typecheck", "npm run test"],
    "tier1": [],
    "tier2": [],
    "risk_triggers": [
      {
        "name": "deps",
        "patterns": ["apps/tactical-grid-ambiguous/package.json"],
        "tier": "tier1"
      },
      {
        "name": "engine",
        "patterns": ["apps/tactical-grid-ambiguous/src/**"],
        "tier": "tier1"
      }
    ],
    "max_verify_time_per_milestone": 600
  },
  "workers": {
    "codex": {
      "bin": "codex",
      "args": ["exec", "--full-auto", "--json"],
      "output": "jsonl"
    },
    "claude": {
      "bin": "claude",
      "args": ["-p", "--output-format", "json", "--dangerously-skip-permissions"],
      "output": "json"
    }
  }
}
